{
  "name": "web-ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server:dev": "NODE_ENV=development webpack-dev-server --host 0.0.0.0 --config webpack.config.js --inline --progress --colors --watch --hot --content-base ./",
    "server:prod": "NODE_ENV=production webpack -p --config webpack.config.js --progress --profile --display-reasons --colors --display-error-details --display-cached",
    "build": "npm run server:prod",
    "start": "npm run server:dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^3.4.1",
    "webpack-dev-server": "^2.6.1"
  }
}
